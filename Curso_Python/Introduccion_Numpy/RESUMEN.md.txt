¿Qué es NumPy?

NumPy es la librería fundamental para cálculo numérico en Python. Su estructura principal es el ndarray, un array multidimensional mucho más rápido y eficiente que las listas normales de Python.

Función	Qué hace	Ejemplo
np.random.rand()	Genera números decimales aleatorios entre 0 y 1 con distribución uniforme	np.random.rand(2,3)
np.random.randn()	Genera números aleatorios siguiendo una distribución normal (media 0, desviación 1)	np.random.randn(3,3)
np.random.randint()	Genera números enteros aleatorios dentro de un rango	np.random.randint(0,10,(2,2))
np.random.random()	Genera un array de decimales aleatorios entre 0 y 1	np.random.random((2,2))
np.random.choice()	Selecciona elementos aleatorios de una lista o array	np.random.choice([1,2,3], size=2)
np.random.shuffle()	Mezcla los elementos de un array modificando el original	np.random.shuffle(a)
np.random.permutation()	Devuelve una copia mezclada del array sin modificar el original	np.random.permutation(a)
np.random.seed()	Fija una semilla para obtener siempre los mismos números aleatorios	np.random.seed(42)
np.random.uniform()	Genera números decimales aleatorios dentro de un intervalo específico	np.random.uniform(0,10,5)
np.random.normal()	Genera números aleatorios con distribución normal personalizada	np.random.normal(0,1,5)
np.random.binomial()	Simula resultados tipo éxito/error repetidos varias veces	np.random.binomial(10,0.5,5)
np.random.poisson()	Genera eventos aleatorios que ocurren en intervalos de tiempo o espacio	np.random.poisson(5,10)
np.random.default_rng()	Crea un generador aleatorio moderno recomendado por NumPy	rng = np.random.default_rng()
rng.integers()	Genera enteros aleatorios usando el generador moderno	rng.integers(0,10,(3,3))







1. Crear arrays
import numpy as np
Desde listas
a = np.array([1, 2, 3])
b = np.array([[1, 2], [3, 4]])
Arrays especiales
np.zeros((2,3))      # matriz de ceros
np.ones((3,3))       # matriz de unos
np.empty((2,2))      # sin inicializar
np.eye(3)            # matriz identidad
np.full((2,2), 7)    # llena de un valor
Rangos y secuencias
np.arange(0, 10, 2)      # [0 2 4 6 8]
np.linspace(0, 1, 5)     # 5 valores entre 0 y 1
2. Propiedades de un array
a.shape      # dimensiones
a.ndim       # número de dimensiones
a.size       # número total de elementos
a.dtype      # tipo de dato
a.itemsize   # bytes por elemento

Ejemplo:

a = np.array([[1,2,3],[4,5,6]])
print(a.shape)   # (2,3)
3. Indexación y slicing
Acceso básico
a[0]
a[1,2]
Subarrays
a[0:2]
a[:,1]
a[1,:]
Indexación booleana
a[a > 3]
4. Cambiar forma del array
reshape
a.reshape((2,3))
flatten / ravel
a.flatten()   # copia
a.ravel()     # vista
transpose
a.T
np.transpose(a)
5. Operaciones matemáticas
Operaciones elemento a elemento
a + b
a - b
a * b
a / b
a ** 2
Funciones matemáticas
np.sqrt(a)
np.exp(a)
np.sin(a)
np.log(a)
6. Estadística básica
np.mean(a)
np.median(a)
np.std(a)
np.var(a)
np.min(a)
np.max(a)
np.sum(a)
Por ejes
np.sum(a, axis=0)   # columnas
np.sum(a, axis=1)   # filas
7. Álgebra lineal

Módulo:

np.linalg
Producto matricial
a @ b
np.dot(a, b)
Determinante
np.linalg.det(a)
Inversa
np.linalg.inv(a)
Valores y vectores propios
np.linalg.eig(a)
8. Concatenar y dividir arrays
Concatenar
np.concatenate([a,b])
np.vstack([a,b])
np.hstack([a,b])
Dividir
np.split(a, 2)
9. Arrays aleatorios
np.random.rand(3,3)
np.random.randn(3,3)
np.random.randint(0,10,5)
Semilla
np.random.seed(42)
10. Filtrado y condiciones
a[a > 5]
where
np.where(a > 5, 1, 0)
11. Copias y vistas
Vista (comparte memoria)
b = a.view()
Copia real
b = a.copy()
12. Funciones muy usadas
Función	Descripción
np.array()	Crear array
np.arange()	Rangos
np.linspace()	Intervalos
np.reshape()	Cambiar forma
np.sum()	Suma
np.mean()	Media
np.max()	Máximo
np.min()	Mínimo
np.where()	Condiciones
np.concatenate()	Unir arrays
np.dot()	Producto matricial
13. Broadcasting (muy importante)

Permite operar arrays de distinto tamaño automáticamente.

Ejemplo:

a = np.array([[1],[2],[3]])
b = np.array([10,20,30])

print(a + b)

Resultado:

[[11 21 31]
 [12 22 32]
 [13 23 33]]
14. Ejemplo completo
import numpy as np

a = np.array([[1,2,3],
              [4,5,6]])

print(a.shape)

print(a.mean())

print(a[:,1])

b = a * 2

print(b)
15. Buenas prácticas
Usa arrays NumPy en lugar de listas para cálculo numérico.
Evita bucles for cuando puedas vectorizar operaciones.
Usa dtype apropiados para ahorrar memoria.
Aprovecha broadcasting y slicing.